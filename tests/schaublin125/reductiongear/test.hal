setexact_for_test_suite_only

loadrt threads name1=fast period1=100000000  # 100ms
#loadrt threads name1=fast period1=100000  # 1ms

loadrt streamer cfg=u depth=10
loadrt sampler cfg=uu depth=10
loadrt schaublin125

addf streamer.0 fast
addf sampler.0 fast
addf schaublin125.0 fast

# net lowgear  streamer.0.pin.0 schaublin125.0.select-low
net speedin  streamer.0.pin.0 schaublin125.0.speed-in sampler.0.pin.0
net speedout schaublin125.0.speed-out sampler.0.pin.1

loadusr -w sh runstreamer
start
loadusr -w halsampler -n 2
