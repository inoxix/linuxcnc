component andor "Test component of an and-or gate";

pin in bit in0;
pin in bit in1;
pin in bit in2;
pin out bit out;

function _ nofp;

license "GPL";
;;

FUNCTION(_) {
	out = (in0 && in1) || in2;
}
